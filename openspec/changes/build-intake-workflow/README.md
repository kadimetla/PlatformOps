# build-intake-workflow

Gateway schemas (Scope, Actor, IntakeRequest/IntakeDecision) and the intake intent-classification workflow -- the first buildable slice of docs/INTAKE_HITL_ROUTING.md's design, testable with a scripted fake chat model and no cloud/IdP dependencies
