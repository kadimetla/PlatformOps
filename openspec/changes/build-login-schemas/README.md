# build-login-schemas

Actor/ExecutionGrant/ApprovalGrant/Capability schemas and OIDC ID-token claims parsing+validation -- the first buildable slice of docs/ACCESS_POLICY_AND_IAM_DISCOVERY.md's login flow, testable with a locally-signed test JWT and no real IdP
