## Status
Entry point / document map. Created new on this branch (per
`CLAUDE.md`'s process) — `docs/` did not exist here before. Points to
docs that carry real content; this file itself holds no design
reasoning, only the map.

## Document map
| Doc | Covers | Status |
|---|---|---|
| [TERRAFORM_MCP_SERVER.md](TERRAFORM_MCP_SERVER.md) | HashiCorp `terraform-mcp-server` integration: verified CLI/env, tool inventory, HCP Terraform workspace concepts | Verified against current upstream docs; one code bug found and fixed |
| [INTAKE_HITL_ROUTING.md](INTAKE_HITL_ROUTING.md) | Request intake classification, bounded caller-side clarification (return-and-re-invoke, no in-graph interrupt), `<org>:<bu>` → project → workspace scope model, deterministic per-scope routing, request-time cloud access flow (AWS/Azure/GCP) | Corrected/extended in place since the 2026-07-27 deep-dive explore, including a 2026-07-31 fix reconciling its `gateway/policy.py` with `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s `gateway/policy/` (now one package, not a file/directory name collision — and now real code, see that doc's row); classification (`workflows/intake/`) is real, and **`resolve_route` implemented 2026-08-07** (`workflows/intake/nodes.py`, `openspec/changes/build-intake-dispatcher/`) for the `compliance_check` intent only — no `Scope` on `IntakeRequest`, no per-`org_bu` `POLICY` registry/YAML, no `gateway/dispatcher.py` module, no `actor.execution_grants` gating; the full per-scope routing pipeline this doc designs remains not implemented |
| [ACCESS_POLICY_AND_IAM_DISCOVERY.md](ACCESS_POLICY_AND_IAM_DISCOVERY.md) | Login-time access discovery (OIDC login → provider principal resolution → per-cloud entitlement discovery → capability normalization → session grants), the capability ladder, `effective_access = min(grant, ceiling)`, discovery/execution/bootstrap identity separation, new-project bootstrap, `gateway/policy/` registry | Designed only, provider APIs verified against current docs 2026-07-28; three gaps found and corrected during verification; **`gateway/policy/ceiling.py` (the `effective_access` evaluator) implemented 2026-07-31** — login/discovery/normalization itself still not implemented, no live policy YAML data exists |
| [EXECUTION_CREDENTIALS.md](EXECUTION_CREDENTIALS.md) | Execution-time credential acquisition: runtime root identity (Layer 0), per-workspace execution identities + delegation (Layer 1), JIT short-lived tokens (Layer 2), the Azure narrowing asymmetry, the approval gate (self-looping interrupt node, PlatformOps-native approval authority, staleness/revocation rules), the executor sub-graph (CCAPI-vs-Terraform fork, bounded polling not interrupt, three-class failure taxonomy, `approval_digest` binding covering plan/policy/identity/allow-list drift in one check), no-tokens-in-graph-state rule, `CloudAccessAdapter` Protocol unifying discovery/credential methods across providers with a contract-first-across-three/depth-on-AWS-only build strategy | Designed only; mechanisms standard but flagged verify-before-build; **`ApprovalRequest`/`ApprovalRecord` schema implemented 2026-07-31** (`gateway/approval.py`) — approval gate node and executor itself still not implemented |
| [INQUIRY_WORKFLOW.md](INQUIRY_WORKFLOW.md) | Read-only workflow execution for the `inquiry` intent: the three inquiry shapes (scoped describe / self enumeration / unscoped advisory), the capability ladder reused as inspection depth (not just mutation ceiling), per-request (not per-actor) least-privilege credentials, the tier-keyed execution-identity registry extension, enumeration protection (uniform not-found/not-authorized response), the escalation boundary into a fresh provision request, `InquiryRecord` evidence | Designed only; no inquiry workflow code exists yet |
| [CLOUD_STACK_CATALOG.md](CLOUD_STACK_CATALOG.md) | Canonical reusable provisioning-content contract: Cloud Stack Registry vs. authorized catalog vs. rebuildable search index; `CloudStackDefinition`/immutable signed `CloudStackRelease`/scoped `CloudStackPublication` with its encryption envelope/target-bound `CloudStackDeployment`/fresh provider `PlanResult`; global/sector/org/org+BU publication, deterministic sector-policy intersection, authorization-first exact/full-text/optional-semantic lookup, promotion/revocation/evidence, and the rule that provider plans are never promoted or reused | Designed 2026-08-16; current `PROFILE_REGISTRY` and reviewed `aws-static-web/topology.yaml` are the compatibility seed only. No persistent registry, artifact/key service, promotion workflow, or search index exists; semantic search is explicitly deferred until retrieval evaluation justifies it |
| [PROVISION_WORKFLOW.md](PROVISION_WORKFLOW.md) | New-stack vs. existing-stack provisioning: identical access flow for both, the CCAPI-vs-plan-engine diffing asymmetry, concurrent drift plus artifact and toolchain identity extending `approval_digest` to seven inputs, action-verb allow-list extension, template-first IaC generation, and four canonical toolchains (`ccapi`/`hcp_terraform`/`opentofu_local`/`terraform_local`). The local pair shares one runner contract/two-phase credential spine but seals exact engine/version/platform/dependency-lock/backend/state-owner identity and never reuses plans across engines; includes backend credential-leak rules and admin-gated Terraform→OpenTofu ownership migration | Partially real: request preflight reaches a typed `ProvisionDraft`; topology execution through evidence remains designed. Artifact provenance is `template_version` or `topology_digest`; CCAPI's native-diff gap and both local engines' minimum supported versions remain verify-before-build; Terraform saved-plan/S3-lock behavior verified 2026-08-16; flags missing `provision-infra` Paths C/D, neither applied yet |
| [INTERACTION_LAYER.md](INTERACTION_LAYER.md) | First doc addressing the human-interaction layer: TUI-first + device-code login (resolves `build-login-schemas`'s open redirect-vs-device-code question), a transport-agnostic event envelope wrapping already-designed models (`IntakeDecision`/`ApprovalRequest`/`ExecutionRecord`/`vibe_diff`), Rich-over-Textual for the first slice, the documented future web path (AG-UI/CopilotKit/A2UI), CopilotKit Runtime's proxy pattern (`HttpAgent` dev-only vs. `selfManagedAgents` for production), and `HITLEvent` (`interaction/events.py`, not yet built — relocated out of `gateway/` to keep `AUTH_BOUNDARY.md`'s request/auth/policy boundary from picking up a UI-rendering dependency) mapped onto **AG-UI's native interrupt mechanism** (`RunFinished` interrupt outcome + `resume` array) — corrected in place from an earlier same-day pass that wrongly concluded HITL must ride `Custom` events | Designed and partially real; AG-UI/A2UI top-level claims verified 2026-07-30, event taxonomy/Runtime/interrupt-mechanism details verified 2026-07-31 (source docs internally inconsistent on the interrupt mechanism's stability — re-verify before building); **`interaction/events.py` implemented 2026-07-31** — `PlatformOpsEvent`/`HITLEvent`/`HITLResponse` are real, also required adding `gateway/approval.py` and `ActorRef`; **`interaction/tui.py` and `interaction/agui.py` also implemented 2026-07-31** (stdlib terminal renderer, AG-UI interrupt/resume adapter); the CLI itself moved to `transports/cli.py` same day — see `TRANSPORTS.md`, `interaction/` stays presentation-only; **corrected 2026-08-07**: "A2UI later, fixed-schema first" recommendation overridden — `interaction/a2ui.py` and a real web server (`transports/http.py`) now exist, no CopilotKit runtime by deliberate choice (see `WEB_CHAT_APP.md`'s Scope Decision) |
| [TRANSPORTS.md](TRANSPORTS.md) | Transport boundary for CLI/TUI, HTTP/SSE, WebSocket remote TUI, Teams, and Google Chat: transports normalize/render channel-specific messages only, while gateway/workflows keep auth, policy, classification, approval, and execution behavior. Also captures the tested remote-TUI AG-UI protocol-state slice: user run inputs, interrupt observation, pending-interrupt blocking, all-open-interrupt resume enforcement, and thread ownership checks | Designed and partially real; `transports/cli.py` is the local CLI transport, and **`transports/remote_tui.py` implemented 2026-07-31** as a no-network protocol helper for future WebSocket/SSE clients. **`transports/http.py` implemented 2026-08-07** — real FastAPI/SSE server, single-user/local-dev session handling, see `WEB_CHAT_APP.md`. WebSocket client/server, Teams adapter, and Google Chat adapter still don't exist |
| [WEB_CHAT_APP.md](WEB_CHAT_APP.md) | Milestone 1 of the browser chat app: `interaction/a2ui.py` (A2UI dynamic rendering, wire shape verified against the real installed `@a2ui/web_core` package, not assumed), `transports/http.py` (FastAPI/SSE backend, one `POST /runs` endpoint for both new turns and resumes), the provider-neutral `ChatLiteLLM` boundary selected by `PLATFORMOPS_MODEL`, and the `frontend/` scaffold (Vite + React, `@ag-ui/client` + `@a2ui/react`, Node pinned via Volta). Deliberately skips CopilotKit's Node Runtime — the ask was AG-UI + A2UI, not CopilotKit specifically | Real (`openspec/changes/build-agui-a2ui-transport/`); backend tested (pytest) and smoke-tested with a real running server + Vite dev proxy; frontend compiles/builds clean against real installed packages but was never rendered in an actual browser (none available in the build environment) — the one remaining manual verification step |
| [PLATFORMOPS_HARNESS.md](PLATFORMOPS_HARNESS.md) | The one runtime object every transport calls into instead of duplicating orchestration logic: `PlatformOpsHarness.start_run()`/`resume_clarification()` validate the session, call intake, and wrap results as `HITLEvent`/`PlatformOpsEvent`. Scoped narrowly and explicitly: no dispatch by scope/policy past `resolve_route`'s intent-only routing (**corrected 2026-08-07** — `resolve_route` now resolves `route`/`ready_to_route` for `compliance_check`, surfaced in the event payload, but nothing here calls the workflow it names), no approval resume (no checkpointed workflow exists). Verified against OpenClaw's own docs — corrects a naming inversion: what OpenClaw calls a "harness" (a narrow, swappable single-turn executor) is what PlatformOps's toolchain selection would be, not this object, which plays OpenClaw's runtime-core role. Also designs per-run tenant binding, multi-session/multi-actor invocation, `ThreadState`/`RunState`/`EventRecord`, approver-vs-requester resume authorization, and one-active-run-per-thread concurrency | Real for `start_run`/`resume_clarification`; optional per-run `ScopeHint` is real as of 2026-08-14 and preserved across intake clarification. Durable `RunContext`, `ThreadState`/`RunState`/`EventRecord`, and multi-actor approval authorization remain designed only; no persistence exists |
| [MEMORY_ARCHITECTURE.md](MEMORY_ARCHITECTURE.md) | Six memory types translated from OpenClaw's model (working/episodic/semantic/procedural/policy/evidence), with the one rule that matters stated explicitly: policy memory is authoritative, every other type is context only. Grounds "skills are procedural memory" against the actual `SKILL.md` files (all three already self-describe as procedures). Maps each type onto `WORKFLOW_LIFECYCLE_PATTERN.md`'s seven steps. Treats the identity/behavior-as-YAML idea as documentation of already-enforced rules, not a new config surface to build | Designed only; OpenClaw claims verified against current docs 2026-07-31; working memory (`harness/core.py`) and policy memory (`gateway/policy/ceiling.py`) are the only two memory types with real code, everything else is either designed-only or unbuilt |
| [IDP_SELECTION.md](IDP_SELECTION.md) | IdP research narrowed to this project's two real requirements — device-code grant (RFC 8628) and SCIM-to-AWS-Identity-Center including groups — across Keycloak/Authentik/ZITADEL; **Authentik recommended for MVP** (native SCIM covers groups, matching this design's group-driven `execution_grants`/`approval_grants` directly), Keycloak documented as the alternative if ecosystem maturity outweighs the SCIM fit; adds Authentik deployment options (Docker Compose vs. Kubernetes, no managed/hosted tier) and its PostgreSQL/Redis/file-storage boundary vs. PlatformOps's own session/grant storage | Research only, verified against current sources 2026-07-30, deployment-options and data-storage sections added and verified 2026-07-31; corrects `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s Principal-ID Mapping table in place; corroborates `AUTH_BOUNDARY.md`'s storage boundary; **`deploy/authentik/` + `scripts/authentik-smoke.sh` local manual-smoke scaffolding implemented 2026-07-31** (`AUTHENTIK_TAG=2026.5.6`, verified same day); no live IdP deployed |
| [OPS_IAM_FLOW.md](OPS_IAM_FLOW.md) | Operational IAM access-request lifecycle: bootstrap-defined requestable roles, user request, deterministic eligibility checks, approver gate, narrow Authentik group membership mutation, optional SCIM/provider propagation, session refresh, revocation/expiry, and evidence. Keeps execution grants provider-discovered and approval grants PlatformOps-native | Designed 2026-08-19; no self-service access-request workflow, Authentik admin client, SCIM reconciliation job, or provider-group mutation adapter exists |
| [OPEN_SOURCE_IGA_OPTIONS.md](OPEN_SOURCE_IGA_OPTIONS.md) | Open-source/open-core Saviynt-like options for access governance: midPoint, OpenIAM, Apache Syncope, Soffid, and why Authentik remains the IdP/group boundary rather than the IGA workflow engine. Defines the external-IGA path versus PlatformOps's internal MVP access-request flow | Research summary captured 2026-08-19; no tool integrated. Re-verify licenses, APIs, deployment shape, and connector fit before implementation |
| [MIDPOINT_IGA_DEEP_DIVE.md](MIDPOINT_IGA_DEEP_DIVE.md) | Deep dive on midPoint as the external IGA candidate: separate self-service UI, Authentik/IdP login options, requestable PlatformOps project/workspace roles, approval/implementation ownership, PlatformOps chat handoff, and session-refresh grant consumption | Research/design note captured 2026-08-19; no midPoint instance, connector, REST integration, or PlatformOps adapter exists |
| [GUEST_ACCESS_REQUEST_CHAT.md](GUEST_ACCESS_REQUEST_CHAT.md) | Chat-first access request flow for users without PlatformOps operational grants: anonymous vs authenticated guest vs authorized user, limited `access_request` route, non-enumerating target handling, midPoint/internal backend handoff, and security invariants | Designed 2026-08-19; no `access_request` intent, guest chat route, backend adapter, or workflow exists |
| [ACCESS_REQUEST_IMPLEMENTATION_PLAN.md](ACCESS_REQUEST_IMPLEMENTATION_PLAN.md) | Build plan for chat-driven permission requests: intent/route gate, schemas, requestable-access catalog, LangGraph preflight, manual backend, status lookup, midPoint backend spike, sync/callback, optional internal IGA backend, and evidence foundation | Implementation plan captured 2026-08-20; no access-request code exists |
| [AUTH_BOUNDARY.md](AUTH_BOUNDARY.md) | Auth as a security boundary, not agent workflow behavior: the `gateway/auth/` module layout, why workflows receive an already-authenticated `ActorSession` rather than authenticating themselves, and grant resolution resolved as a plain deterministic pipeline — explicitly not LangGraph-shaped, since none of its value (checkpointing/HITL/LLM routing) applies to a fixed-order sequence | Partially real — `schemas.py`/`claims.py`/`grants.py`/`sessions.py`/`login.py` and a `gateway.auth.cli` smoke entry point exist; `grants.py` mints approval grants only, never execution grants, per `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s precedence rule; `providers/` (`CloudAccessAdapter` implementations) do not exist. **2026-07-31**: `gateway/policy/`, `gateway/approval.py`, and the sibling `interaction/` package now exist too — see this doc's Real vs. Designed table |
| [BOOTSTRAP_WORKFLOW.md](BOOTSTRAP_WORKFLOW.md) | How the identities get created: the bootstrap ladder (Level -1 manual → org/BU as PR-reviewed config → automated project/workspace bootstrap), bootstrap-as-provision with a disjoint identity-only allow-list, never LLM-routed, registry-written-last with lifecycle state, per-cloud escalation ceilings (permissions boundaries / role allow-lists), teardown deferred as a separate path; **2026-08-14**: full `access_templates.yaml` field schema (`templates.<name>.tiers.<tier>` → max_capability/execution_identity_name pattern/required_approvals ≥1), plus the composite-ceiling resolution (`workspace_ceiling = min(org_bu_policy.ceiling, registry.max_capability)`, surfaced via a new `WorkflowAuthContext` snapshot) closing the gap between the registry's scalar `max_capability` and `ceiling.py`'s `CeilingEntry`, and a new bootstrap-verify consistency check (template/registry/execution-identity ceilings must agree) | Designed only; per-cloud ceiling mechanisms flagged verify-before-build; ceiling-wiring now resolved in design but not implemented — changes `gateway/policy/ceiling.py`'s existing contract; nothing implemented |
| [WORKFLOW_LIFECYCLE_PATTERN.md](WORKFLOW_LIFECYCLE_PATTERN.md) | Cross-cutting naming layer over the workflow docs above: every workflow follows `intake -> context -> plan -> approval? -> executor? -> evidence -> reporting`, steps may be skipped but never reordered (new rule, stated here first); per-workflow mapping for provision/inquiry/bootstrap; flags two real gaps left as gaps — no bootstrap-specific evidence record, no reporting layer designed at all | New synthesis, no design changed; `workflows/intake/` has the real two-node `classify_workflow -> resolve_route` graph, and `workflows/provision/` now has request-preflight nodes ending at `ProvisionDraft`; topology/context/plan/approval/executor/evidence/reporting remain docs-only |
| [APPLICATION_PROVISIONING.md](APPLICATION_PROVISIONING.md) | The first real `workflows/provision/` target: S3+CloudFront static frontend + an existing EKS cluster's API backend, AWS-only, `opentofu_local` for the MVP; `terraform_local` is a later adapter over the same sealed local-runner contract, never fallback. Full reference architecture and workflow boundaries reconciled against the shared provisioning/credential/bootstrap designs | Designed only, three AWS claims verified 2026-08-14; dual-control Kubernetes allow-list, EKS auth, and the single `provision-infra` entry point are resolved; local Terraform remains designed but outside the first build |
| [COMPOSABLE_PROVISIONER.md](COMPOSABLE_PROVISIONER.md) | The composable-provisioner target shape and implementation walkthrough: reviewed and dynamically authored topology revisions, typed unit/resource DAGs, deterministic validation, runtime compilation inside a fixed lifecycle, revision-scoped state, reviewed rendering, real-plan checks, sealing and topology-bound approval. Also evaluates planner harnesses: Pi/Node and Pydantic AI rejected; plain LangGraph/`create_agent` remains the bounded-runtime default; Deep Agents retained conditionally with a stripped runtime profile and a separate Slice 15 sandboxed Level 2 module/renderer-authoring role | Designed target with the basic `TopologySpec` loader/structural validator already real; auth/credentials/approval/apply remain fixed parent stages; Slices 13/14/15 disabled pending independent acceptance. **2026-08-16 Deep Agents decision:** no runtime adoption without an equal-case A/B proving context/delegation benefit and zero extra tool exposure; offline credential-free authoring may be accepted independently because it uses the harness capabilities rather than removing them |
| [PROVISION_IMPLEMENTATION_PLAN.md](PROVISION_IMPLEMENTATION_PLAN.md) | Sixteen independently testable delivery slices plus a contract-first vertical implementation path from today's real `ProvisionDraft` through trusted context, release/revision loading, unit execution, canonical plan/render contracts, saved-plan policy/sealing, approval/apply, verification/evidence, and final route activation; includes separate authoring-time Slice 15 and Cloud Stack Registry/catalog Slice 16 | Slices 1-5 plus Slice 7's basic structural validator are represented in code: handler routing still ends in a non-executable draft, and the profile/topology loader is not invoked by that graph; compiler, IaC, credentials, approval, cloud actions, offline authoring harness, and Cloud Stack services remain disabled. **2026-08-16 audit:** duplicate `DeploymentPlan` shapes reconciled; Slice 9 blocked on an explicit policy decision for pre-approval S3 lockfile writes |
| [APPROVAL_GATE.md](APPROVAL_GATE.md) | Implementation of the approval pause, complementing EXECUTION_CREDENTIALS.md's design: verified LangGraph re-execution hazard and the request/gate node split, field-name corrections against real schemas, seven-input approval binding including artifact and toolchain identity, transport wiring, server-side validation, inbox/panel split, and persistence components | Designed only, with schemas + AG-UI adapters already real; re-execution behaviour verified empirically 2026-08-14; no gate node, no `resume_approval`, no persistence, no inbox UI |
| [AWS_PROVIDER_DISCOVERY.md](AWS_PROVIDER_DISCOVERY.md) | AWS half of `CloudAccessAdapter.resolve_principal`/`resolve_execution_grants`: the boto3-for-discovery-only decision (vs. this project's MCP-only provisioning path), the 5-call `sso-admin`/`identitystore` sequence (`ListInstances` once, `GetUserId`, `ListGroupMembershipsForMember`, `ListAccountAssignmentsForPrincipal` per user+group, `DescribePermissionSet` per unique permission set), permission-set-`Name` capability normalization via a new `capability_mappings.yaml` (**corrected 2026-08-14**: not `access_templates.yaml`, which was already claimed — see BOOTSTRAP_WORKFLOW.md's row), the discovery identity's required IAM permissions, and the still-open `AccountId -> Scope` reverse-lookup dependency on `project_registry.yaml` | Designed only, AWS API shapes verified against current docs 2026-08-14; no code, no `gateway/auth/providers/` directory, no `capability_mappings.yaml`/`project_registry.yaml` |

## How this relates to the existing docs
`AGENTS.md` names this file as the entry point once a non-trivial
design gets written down (`## Conventions` → `docs/`). A more advanced
exploration of the same LangGraph direction exists on
`design/harness-architecture`, which has its own `docs/HARNESS_DESIGN.md`
— that branch is not merged here and its document map is a separate,
unrelated index; don't confuse the two.
