## Status
Entry point / document map. Created new on this branch (per
`CLAUDE.md`'s process) — `docs/` did not exist here before. Points to
docs that carry real content; this file itself holds no design
reasoning, only the map.

## Document map
| Doc | Covers | Status |
|---|---|---|
| [TERRAFORM_MCP_SERVER.md](TERRAFORM_MCP_SERVER.md) | HashiCorp `terraform-mcp-server` integration: verified CLI/env, tool inventory, HCP Terraform workspace concepts | Verified against current upstream docs; one code bug found and fixed |
| [INTAKE_HITL_ROUTING.md](INTAKE_HITL_ROUTING.md) | Request intake classification, bounded caller-side clarification (return-and-re-invoke, no in-graph interrupt), `<org>:<bu>` → project → workspace scope model, deterministic per-scope routing, request-time cloud access flow (AWS/Azure/GCP) | Designed only, corrected/extended in place since the 2026-07-27 deep-dive explore, including a 2026-07-31 fix reconciling its `gateway/policy.py` with `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s `gateway/policy/` (now one package, not a file/directory name collision — and now real code, see that doc's row); dispatcher/full routing pipeline itself still not implemented |
| [ACCESS_POLICY_AND_IAM_DISCOVERY.md](ACCESS_POLICY_AND_IAM_DISCOVERY.md) | Login-time access discovery (OIDC login → provider principal resolution → per-cloud entitlement discovery → capability normalization → session grants), the capability ladder, `effective_access = min(grant, ceiling)`, discovery/execution/bootstrap identity separation, new-project bootstrap, `gateway/policy/` registry | Designed only, provider APIs verified against current docs 2026-07-28; three gaps found and corrected during verification; **`gateway/policy/ceiling.py` (the `effective_access` evaluator) implemented 2026-07-31** — login/discovery/normalization itself still not implemented, no live policy YAML data exists |
| [EXECUTION_CREDENTIALS.md](EXECUTION_CREDENTIALS.md) | Execution-time credential acquisition: runtime root identity (Layer 0), per-workspace execution identities + delegation (Layer 1), JIT short-lived tokens (Layer 2), the Azure narrowing asymmetry, the approval gate (self-looping interrupt node, PlatformOps-native approval authority, staleness/revocation rules), the executor sub-graph (CCAPI-vs-Terraform fork, bounded polling not interrupt, three-class failure taxonomy, `approval_digest` binding covering plan/policy/identity/allow-list drift in one check), no-tokens-in-graph-state rule, `CloudAccessAdapter` Protocol unifying discovery/credential methods across providers with a contract-first-across-three/depth-on-AWS-only build strategy | Designed only; mechanisms standard but flagged verify-before-build; **`ApprovalRequest`/`ApprovalRecord` schema implemented 2026-07-31** (`gateway/approval.py`) — approval gate node and executor itself still not implemented |
| [INQUIRY_WORKFLOW.md](INQUIRY_WORKFLOW.md) | Read-only workflow execution for the `inquiry` intent: the three inquiry shapes (scoped describe / self enumeration / unscoped advisory), the capability ladder reused as inspection depth (not just mutation ceiling), per-request (not per-actor) least-privilege credentials, the tier-keyed execution-identity registry extension, enumeration protection (uniform not-found/not-authorized response), the escalation boundary into a fresh provision request, `InquiryRecord` evidence | Designed only; no inquiry workflow code exists yet |
| [PROVISION_WORKFLOW.md](PROVISION_WORKFLOW.md) | New-stack vs. existing-stack provisioning: identical access flow for both, the CCAPI-vs-Terraform diffing asymmetry, concurrent-drift protection (`current_state_fingerprint`/`template_version` extending `approval_digest` to six inputs), action-verb allow-list extension for gating delete separately from create/update, template-first IaC generation (Level 1 render / Level 2 template-library PR / Level 3 executor — never freshly-generated unreviewed IaC), three toolchains (`ccapi`/`hcp_terraform`/`opentofu_local`) with distinct credential footprints, `opentofu_local`'s S3-backend state design and two-phase (plan/apply) credential acquisition, backend-config-credential-leak rule (verified), admin-gated Terraform→OpenTofu workspace migration | Designed only; CCAPI's lack of a native diff primitive and OpenTofu's `use_lockfile` version floor both flagged verify-before-build; corrects `skills/provision-infra/SKILL.md`'s free-form-drafting step and flags its missing Path C, neither applied there yet |
| [INTERACTION_LAYER.md](INTERACTION_LAYER.md) | First doc addressing the human-interaction layer: TUI-first + device-code login (resolves `build-login-schemas`'s open redirect-vs-device-code question), a transport-agnostic event envelope wrapping already-designed models (`IntakeDecision`/`ApprovalRequest`/`ExecutionRecord`/`vibe_diff`), Rich-over-Textual for the first slice, the documented future web path (AG-UI/CopilotKit/A2UI), CopilotKit Runtime's proxy pattern (`HttpAgent` dev-only vs. `selfManagedAgents` for production), and `HITLEvent` (`interaction/events.py`, not yet built — relocated out of `gateway/` to keep `AUTH_BOUNDARY.md`'s request/auth/policy boundary from picking up a UI-rendering dependency) mapped onto **AG-UI's native interrupt mechanism** (`RunFinished` interrupt outcome + `resume` array) — corrected in place from an earlier same-day pass that wrongly concluded HITL must ride `Custom` events | Designed and partially real; AG-UI/A2UI top-level claims verified 2026-07-30, event taxonomy/Runtime/interrupt-mechanism details verified 2026-07-31 (source docs internally inconsistent on the interrupt mechanism's stability — re-verify before building); **`interaction/events.py` implemented 2026-07-31** — `PlatformOpsEvent`/`HITLEvent`/`HITLResponse` are real, also required adding `gateway/approval.py` and `ActorRef`; **`interaction/tui.py` and `interaction/agui.py` also implemented 2026-07-31** (stdlib terminal renderer, AG-UI interrupt/resume adapter); the CLI itself moved to `transports/cli.py` same day — see `TRANSPORTS.md`, `interaction/` stays presentation-only; no actual web server/CopilotKit runtime exists |
| [TRANSPORTS.md](TRANSPORTS.md) | Transport boundary for CLI/TUI, HTTP/SSE, WebSocket remote TUI, Teams, and Google Chat: transports normalize/render channel-specific messages only, while gateway/workflows keep auth, policy, classification, approval, and execution behavior. Also captures the tested remote-TUI AG-UI protocol-state slice: user run inputs, interrupt observation, pending-interrupt blocking, all-open-interrupt resume enforcement, and thread ownership checks | Designed and partially real; `transports/cli.py` is the local CLI transport, and **`transports/remote_tui.py` implemented 2026-07-31** as a no-network protocol helper for future WebSocket/SSE clients. No HTTP server, SSE endpoint, WebSocket client/server, Teams adapter, or Google Chat adapter exists yet |
| [PLATFORMOPS_HARNESS.md](PLATFORMOPS_HARNESS.md) | The one runtime object every transport calls into instead of duplicating orchestration logic: `PlatformOpsHarness.start_run()`/`resume_clarification()` validate the session, call intake, and wrap results as `HITLEvent`/`PlatformOpsEvent`. Scoped narrowly and explicitly: no routing past classification (no dispatcher exists), no approval resume (no checkpointed workflow exists), no transport wired to it yet (the model-provider decision is still unmade). Verified against OpenClaw's own docs — corrects a naming inversion: what OpenClaw calls a "harness" (a narrow, swappable single-turn executor) is what PlatformOps's toolchain selection would be, not this object, which plays OpenClaw's runtime-core role. Also designs (not built) multi-session/multi-actor invocation — `ThreadState`/`RunState`/`EventRecord`, approver-vs-requester resume authorization, one-active-run-per-thread concurrency | Real for `start_run`/`resume_clarification` (`harness/core.py`, 7 tests); `ThreadState`/`RunState`/`EventRecord` and multi-actor approval authorization designed only, added 2026-07-31, no persistence exists |
| [IDP_SELECTION.md](IDP_SELECTION.md) | IdP research narrowed to this project's two real requirements — device-code grant (RFC 8628) and SCIM-to-AWS-Identity-Center including groups — across Keycloak/Authentik/ZITADEL; **Authentik recommended for MVP** (native SCIM covers groups, matching this design's group-driven `execution_grants`/`approval_grants` directly), Keycloak documented as the alternative if ecosystem maturity outweighs the SCIM fit; adds Authentik deployment options (Docker Compose vs. Kubernetes, no managed/hosted tier) and its PostgreSQL/Redis/file-storage boundary vs. PlatformOps's own session/grant storage | Research only, verified against current sources 2026-07-30, deployment-options and data-storage sections added and verified 2026-07-31; corrects `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s Principal-ID Mapping table in place; corroborates `AUTH_BOUNDARY.md`'s storage boundary; **`deploy/authentik/` + `scripts/authentik-smoke.sh` local manual-smoke scaffolding implemented 2026-07-31** (`AUTHENTIK_TAG=2026.5.6`, verified same day); no live IdP deployed |
| [AUTH_BOUNDARY.md](AUTH_BOUNDARY.md) | Auth as a security boundary, not agent workflow behavior: the `gateway/auth/` module layout, why workflows receive an already-authenticated `ActorSession` rather than authenticating themselves, and grant resolution resolved as a plain deterministic pipeline — explicitly not LangGraph-shaped, since none of its value (checkpointing/HITL/LLM routing) applies to a fixed-order sequence | Partially real — `schemas.py`/`claims.py`/`grants.py`/`sessions.py`/`login.py` and a `gateway.auth.cli` smoke entry point exist; `grants.py` mints approval grants only, never execution grants, per `ACCESS_POLICY_AND_IAM_DISCOVERY.md`'s precedence rule; `providers/` (`CloudAccessAdapter` implementations) do not exist. **2026-07-31**: `gateway/policy/`, `gateway/approval.py`, and the sibling `interaction/` package now exist too — see this doc's Real vs. Designed table |
| [BOOTSTRAP_WORKFLOW.md](BOOTSTRAP_WORKFLOW.md) | How the identities get created: the bootstrap ladder (Level -1 manual → org/BU as PR-reviewed config → automated project/workspace bootstrap), bootstrap-as-provision with a disjoint identity-only allow-list, never LLM-routed, registry-written-last with lifecycle state, per-cloud escalation ceilings (permissions boundaries / role allow-lists), teardown deferred as a separate path | Designed only; per-cloud ceiling mechanisms flagged verify-before-build; nothing implemented |
| [WORKFLOW_LIFECYCLE_PATTERN.md](WORKFLOW_LIFECYCLE_PATTERN.md) | Cross-cutting naming layer over the workflow docs above: every workflow follows `intake -> context -> plan -> approval? -> executor? -> evidence -> reporting`, steps may be skipped but never reordered (new rule, stated here first); per-workflow mapping for provision/inquiry/bootstrap; flags two real gaps left as gaps — no bootstrap-specific evidence record, no reporting layer designed at all | New synthesis, no design changed; grounded against actual repo state 2026-07-31 — `workflows/intake/` is the only workflow directory that exists (one-node classify graph), everything past it is docs-only |

## How this relates to the existing docs
`AGENTS.md` names this file as the entry point once a non-trivial
design gets written down (`## Conventions` → `docs/`). A more advanced
exploration of the same LangGraph direction exists on
`design/harness-architecture`, which has its own `docs/HARNESS_DESIGN.md`
— that branch is not merged here and its document map is a separate,
unrelated index; don't confuse the two.
